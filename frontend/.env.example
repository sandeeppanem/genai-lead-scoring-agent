# Browser configuration only. Do not put AI_GATEWAY_API_KEY or any server secret here.
REACT_APP_API_URL=http://localhost:8000/api

# For the local server-side smoke test, copy this file to .env.local and set:
# AI_GATEWAY_API_KEY=your-vercel-gateway-key
# .env.local is ignored by git. CRA does not expose this key to the browser.
